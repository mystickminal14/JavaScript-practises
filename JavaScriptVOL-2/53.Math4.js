console.log("Exponential Calculation: Create a program that calculates the exponentiation of a base number to a given power using the Math function.s")
const expo=(a)=>{
    return Math.pow(a,2)
}
let y=prompt("give a number")
y= Number.parseInt(y)
console.log("the give power :",expo(y))
console.log(Math.PI)